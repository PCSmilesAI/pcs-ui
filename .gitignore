# Environment files — NEVER commit these
.env
.env.*
.env.local
.env.production
production.env
*.secret
*.secrets
production-secrets.txt
secrets.txt

# Token databases
qbo_tokens.db*

# Data directories (runtime data, not source code)
email_invoices/
output_jsons/
pcs_ai_data/

# Node & Python junk
node_modules/
__pycache__/
*.pyc

# Build artifacts
dist/
build/
out/
.next/
.vite/
.turbo/
.cache/

# Local files not needed in GitHub
log.txt
ui_upload.log
flowstack.log
processed_invoices/

# macOS
.DS_Store
.aider*
*.tar.gz
env
pcs_ui_data/pcs.db*

# Backup folders and copies
pcs-ui dec3 copy/
backup-ui-*/
*copy/

# Debug/test artifacts (should never be committed)
ocr_debug_overlay.png
ocr_output.csv
parsed_invoice*.json
epic_invoice_*.png
epic_invoice_*.json
labeled_epic_invoice_*.json
reprocess_stats.json
vendor_audit_report.json
invoice_queue_backup.json
deps-graph.txt
python-deps.txt
test-*.html
force_refresh.html
~$*
smiles_office_info.xls
chart_of_accounts.rtf
classes.rtf

# BuildLog runtime data
# buildlog.json is gitignored: it grows continuously and contains sensitive
# fields (user_email, transcript_path). It is not source code.
build_audit/buildlog.json
build_audit/buildlog.json.bak
build_audit/buildlog_*.tmp

# Reports are NOT gitignored. They are generated snapshots with no sensitive
# data. Commit them at milestones for client proof-of-work and course material.
# Regenerate anytime with: python3 scripts/render_report.py

# Python
__pycache__/
*.pyc
*.pyo

# macOS
.DS_Store

# Editor
*.swp
*.swo
*~
Desktop
Desktop.pub
*.pub
